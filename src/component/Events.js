import React from 'react';

const Event = () => {
    return (
        <div>
            <h2>Welcome to the Event Page, this is the new repo for me</h2>
        </div>
    );
};

export default Event;
